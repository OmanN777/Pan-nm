1.วิธีตั้งค่าฐานข้อมูล
	-ไปที่โปรแกรมสำหรับ sql
	-ทำการรันไฟล์ sql ตามลามลำดับเพื่อสร้างฐานข้อมูล 
	-sec2_gr7_database.sql
	-sec2_gr7_database(insert).sql
2.วิธีตั้งค่าโปรแกรม
	-ลง nodeJs
	-Unzip/clone project
	-Cd เข้าไปที่ backend
	-ถ้ายังไม่มี node_module ให้ทำการพิมพ์ npm install
	-ตั้งค่า .env ให้ตรงตามฐานข้อมูลตัวเอง
3.การรัน
	-พิมพ์ node index.js
